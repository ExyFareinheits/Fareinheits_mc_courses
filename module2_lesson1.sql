-- =====================================================
-- МОДУЛЬ 1, УРОК 1: Що таке TPS і чому це важливо
-- =====================================================
-- Курс: paid-2 (Ідеальна Оптимізація Minecraft Сервера)

DO $$
DECLARE
  v_module_id TEXT;
  v_course_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  -- Генеруємо UUID
  v_module_id := gen_random_uuid()::text;
  v_lesson_id := gen_random_uuid()::text;
  
  -- Видаляємо старий модуль 1 якщо існує
  DELETE FROM course_lessons WHERE module_id IN (
    SELECT id::text FROM course_modules WHERE course_id = 'paid-2' AND order_index = 1
  );
  DELETE FROM course_modules WHERE course_id = 'paid-2' AND order_index = 1;
  
  -- Створюємо Module 1
  INSERT INTO course_modules (module_id, course_id, title, description, order_index)
  VALUES (
    v_module_id,
    'paid-2',
    'Розуміння TPS та lag причин',
    'Базове розуміння роботи серверу: що таке TPS, як вимірювати продуктивність, основні причини лагів.',
    1
  )
  RETURNING id::text INTO v_course_module_id;
  
  -- Lesson 1
  INSERT INTO course_lessons (
    course_id, 
    module_id, 
    lesson_id, 
    title, 
    type, 
    content, 
    duration, 
    order_index, 
    video_url, 
    is_free_preview
  ) VALUES (
    'paid-2',
    v_course_module_id,
    v_lesson_id,
    'Що таке TPS і чому це важливо',
    'video',
    '# Що таке TPS і чому це важливо

## 🎯 Вступ

TPS (Ticks Per Second) - це серцебиття вашого Minecraft сервера. Розуміння TPS є фундаментом оптимізації.

## 📊 Що таке TPS?

**TPS** - кількість "тіків" (циклів) що сервер виконує за секунду.

### Ідеальні показники:
- ✅ **20 TPS** - ідеальна продуктивність
- ⚠️ **19-18 TPS** - помітні мікрофризи  
- ❌ **<18 TPS** - критичні лаги
- 🚨 **<15 TPS** - гра майже неможлива

## 🔍 Один тік = 50ms

```
1 секунда = 1000ms
20 тіків/секунду = 50ms на тік

Якщо тік займає >50ms → TPS падає!
```

## 🎮 Що відбувається кожен тік?

Кожен тік сервер виконує:
1. Обробка мережі (пакети від гравців)
2. Оновлення ентіті (моби, предмети, гравці)
3. Оновлення tile entities (печі, сундуки)
4. Оновлення чанків (рідини, ріст рослин)
5. Виконання заплановані задачі
6. Обробка redstone
7. Авто-збереження

Якщо всі ці операції займають більше 50ms - TPS падає!

## 📈 Приклади впливу TPS

### 20 TPS (нормально):
- Гравці рухаються плавно
- Моби реагують миттєво
- Редстоун працює точно
- Піч плавить за 10 секунд

### 15 TPS (лаги):
- Гравці телепортуються
- Моби рухаються ривками
- Редстоун спрацьовує неправильно
- Піч плавить за 13+ секунд

## 🛠️ Як перевірити TPS?

### Команди в грі:
```
/tps - показує поточний TPS
/timings - детальний аналіз
/spark tps - розширена інформація
```

## 🔑 Головне

> **TPS = 20** - це не рекомендація, це ВИМОГА!

Кожна мілісекунда важлива. Кожна оптимізація має значення.',
    1200,
    1,
    'https://youtube.com/watch?v=example1',
    true
  );

  RAISE NOTICE 'Module 1, Lesson 1 created successfully!';
  RAISE NOTICE 'Module ID: %', v_course_module_id;
  RAISE NOTICE 'Lesson ID: %', v_lesson_id;
END $$;

-- Перевірка
SELECT 
  m.title as module_title,
  l.order_index,
  l.title as lesson_title,
  l.type
FROM course_modules m
JOIN course_lessons l ON l.module_id = m.id::text
WHERE m.course_id = 'paid-2' AND m.order_index = 1
ORDER BY l.order_index;
