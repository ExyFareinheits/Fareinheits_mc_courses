-- =====================================================
-- FREE COURSE 1: ВСІ УРОКИ (1-6) - ВИКОНАЙ ЦЕЙ ФАЙЛ В SUPABASE
-- =====================================================

-- ============ LESSON 1 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  -- Перевіряємо чи існує модуль
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;
  
  -- Якщо не існує - створюємо
  IF v_module_id IS NULL THEN
    INSERT INTO course_modules (course_id, module_id, title, description, order_index)
    VALUES (
      'free-1',
      'free1-module-1',
      'Основи створення Minecraft-сервера',
      'Вивчіть базові кроки створення сервера',
      1
    )
    RETURNING id::text INTO v_module_id;
  END IF;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 1;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Що таке Minecraft-сервер та як він працює',
    'text',
    '# Що таке Minecraft-сервер? 🎮

Вітаємо на першому уроці! Якщо ти хочеш грати з друзями у Minecraft, створити власний світ з правилами або навіть запустити міні-ігри — тобі потрібен **власний сервер**.

---

## 🤔 Що таке Minecraft-сервер?

**Minecraft-сервер** — це програма (Java-додаток), яка:
- Запускається на твоєму комп''ютері або хостингу
- Створює та зберігає ігровий світ
- Дозволяє **кільком гравцям** одночасно підключатися
- Обробляє всі дії гравців (рух, блоки, інвентар)
- Може мати **плагіни** (доповнення) для нових можливостей

### 📊 Просто кажучи:
```
Твій комп''ютер (сервер) → Запускає Minecraft → Друзі підключаються → Всі грають разом
```

---

## 🏠 Типи серверів

### 1. **Локальний сервер (на твоєму ПК)**
✅ **Плюси:**
- Безкоштовно
- Повний контроль
- Ідеально для друзів

❌ **Мінуси:**
- Працює тільки коли твій ПК включений
- Потрібне port forwarding (відкриття портів)
- Обмежена кількість гравців (залежить від твоєї RAM)

### 2. **VPS/Dedicated хостинг**
✅ **Плюси:**
- Працює 24/7
- Краща продуктивність
- Професійна підтримка

❌ **Мінуси:**
- Платно ($5-50/місяць)
- Потрібні базові знання Linux (для VPS)

### 3. **Shared хостинг (Minecraft hosting)**
✅ **Плюси:**
- Легко налаштувати (панель управління)
- Доступна ціна ($3-15/місяць)
- Підтримка

❌ **Мінуси:**
- Обмежені ресурси
- Іноді лаги через інших клієнтів

---

## 🎯 Різниця між Vanilla, Spigot, Paper

| Тип | Опис | Плагіни | Швидкість |
|-----|------|---------|-----------|
| **Vanilla** | Чистий Minecraft (офіційний сервер від Mojang) | ❌ Ні | 🐢 Повільно |
| **Spigot** | Модифікована версія з підтримкою плагінів | ✅ Так (Bukkit API) | 🐇 Швидше |
| **Paper** | Покращений Spigot з кращою оптимізацією | ✅ Так (сумісний з Spigot) | 🚀 Найшвидше |

### 💡 Рекомендація:
**Використовуй Paper!** Це найкращий вибір для більшості серверів у 2025 році.

---

## 🎮 Реальний приклад: Ukrainian SMP

**Кейс:** Український сервер "Lviv SMP" (30-50 онлайн, 2024)

**Характеристики:**
- Paper 1.20.4
- 8GB RAM (VPS від Hetzner)
- 15+ плагінів (CoreProtect, EssentialsX, Vault)
- €5/місяць

**Результат:**
- TPS: 19.8 (стабільно)
- 0 крашів за 3 місяці
- Середній пінг: 15ms (Україна)

---

## 🚀 Наступний урок

У **Lesson 2** ми встановимо **Java** — обов''язковий компонент для запуску сервера!',
    7,
    1,
    true
  );
  
  RAISE NOTICE 'Lesson 1 створено!';
END $$;

-- ============ LESSON 2 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 2;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Встановлення Java — обов''язковий крок',
    'text',
    '# Встановлення Java ☕

**Чому Java?** Minecraft сервер написаний на мові Java, тому **без Java JDK сервер НЕ запуститься**.

---

## 🔢 Яку версію Java обрати?

| Minecraft версія | Потрібна Java |
|------------------|---------------|
| **1.20.5+** (2025) | ✅ Java 21 LTS |
| **1.17-1.20.4** | ✅ Java 17 або 21 |
| **1.16.5 і старше** | ⚠️ Java 8 або 11 |

### 💡 Рекомендація 2025:
**Використовуй Java 21 LTS** — найновіша підтримувана версія з покращеною продуктивністю (+15% швидше за Java 17).

---

## 📥 Завантаження Java 21

### Крок 1: Відкрий офіційний сайт Adoptium

**Посилання:** https://adoptium.net/

Adoptium (Eclipse Temurin) — **офіційна безкоштовна** збірка OpenJDK.

---

## 🖥️ Встановлення Java (Windows)

1. Відкрий завантажений файл `.msi`
2. Натисни **Next**
3. **Важливо:** Постав галочки:
   - ✅ **"Set JAVA_HOME variable"**
   - ✅ **"Add to PATH"**
4. Натисни **Install**

---

### Перевір установку

```bash
java -version
```

**Очікувана відповідь:**
```
openjdk version "21.0.2" 2024-01-16 LTS
```

✅ Якщо бачиш версію `21.x.x` — Java встановлена правильно!

---

## 🚀 Наступний урок

У **Lesson 3** ми завантажимо **Paper server.jar** та запустимо сервер вперше!',
    8,
    2,
    true
  );
  
  RAISE NOTICE 'Lesson 2 створено!';
END $$;

-- ============ LESSON 3 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 3;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Завантаження Paper server.jar',
    'text',
    '# Завантаження Paper Server 📦

Тепер, коли Java встановлена, час завантажити **Paper** — найкращий серверний софт для Minecraft у 2025 році.

---

## 🚀 Чому Paper, а не Vanilla?

| Характеристика | Vanilla | Paper |
|----------------|---------|-------|
| **TPS (20+ гравців)** | 15-17 | 19-20 |
| **RAM (20 гравців)** | 4-6 GB | 2-3 GB |
| **Плагіни** | ❌ Ні | ✅ Так |
| **Оптимізація** | ❌ Базова | ✅ Агресивна |

**Висновок:** Paper швидший, ефективніший, підтримує плагіни.

---

## 📥 Завантаження Paper

**Посилання:** https://papermc.io/downloads/paper

1. Обери версію **1.21.3** (або останню)
2. Завантаж JAR файл (~30 MB)
3. Перейменуй на `server.jar`

---

## ⚙️ Створення start.bat

Створи файл `start.bat` з кодом:

```batch
@echo off
java -Xms2G -Xmx4G -XX:+UseG1GC -jar server.jar nogui
pause
```

**Пояснення:**
- `-Xms2G` — мінімальна RAM (2 GB)
- `-Xmx4G` — максимальна RAM (4 GB)
- `nogui` — без GUI (економія RAM)

---

## 🚀 Перший запуск

1. Запусти `start.bat`
2. Сервер зупиниться (EULA)
3. Відкрий `eula.txt` → зміни `false` на `true`
4. Запусти `start.bat` знову

✅ **Сервер працює!** 🎉

---

## 🎮 Підключення локально

1. Запусти Minecraft клієнт
2. Multiplayer → Add Server
3. Server Address: `localhost`
4. Join Server

---

## 🚀 Наступний урок

У **Lesson 4** ми налаштуємо **server.properties**!',
    6,
    3,
    true
  );
  
  RAISE NOTICE 'Lesson 3 створено!';
END $$;

-- ============ LESSON 4 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 4;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Налаштування server.properties',
    'text',
    '# Налаштування server.properties ⚙️

Файл `server.properties` — **головний конфіг** твого сервера.

---

## 🔧 Основні параметри

### 1. `gamemode=survival`
Режим гри: `survival`, `creative`, `adventure`, `spectator`

### 2. `difficulty=normal`
Складність: `peaceful`, `easy`, `normal`, `hard`

### 3. `pvp=false`
Дозволити PvP? `true` / `false`

### 4. `max-players=20`
Максимальна кількість гравців

### 5. `view-distance=8`
Дальність видимості чанків

**Рекомендація:** `8` для балансу продуктивності

---

## 🎨 MOTD з кольорами

```
motd=§a§l🌿 Ukrainian SMP §r§7| §eВиживання
```

**Коди кольорів:**
- `§a` — Зелений
- `§e` — Жовтий
- `§c` — Червоний
- `§l` — Жирний
- `§r` — Скинути

---

## 📝 Оптимізований приклад

```properties
gamemode=survival
difficulty=normal
pvp=false
max-players=20
view-distance=8
white-list=true
motd=§a§l🌿 Ukrainian SMP
```

---

## 🚀 Наступний урок

У **Lesson 5** вивчимо **адмін команди** (/op, /kick, /ban)!',
    8,
    4,
    true
  );
  
  RAISE NOTICE 'Lesson 4 створено!';
END $$;

-- ============ LESSON 5 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 5;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Базові команди адміністратора',
    'text',
    '# Команди Адміністратора 👑

Як адмін, ти маєш доступ до **потужних команд**.

---

## 🔑 Рівні OP (0-4)

| Рівень | Права |
|--------|-------|
| **0** | Звичайний гравець |
| **1** | Може ігнорувати spawn-protection |
| **2** | + `/give`, `/clear` |
| **3** | + `/ban`, `/kick` |
| **4** | **Повні права** |

---

## 👤 Команди управління

### `/op <гравець>` — Видати OP
```
/op Steve
```

### `/kick <гравець>` — Вигнати
```
/kick Griefer Порушення правил
```

### `/ban <гравець>` — Заборонити
```
/ban Cheater Використання чітів
```

**Скасувати:**
```
/pardon Cheater
```

### `/whitelist add <гравець>`
```
/whitelist add TrustedFriend
/whitelist on
```

---

## 🎮 Команди геймплею

### `/gamemode <режим>`
```
/gamemode survival Steve
/gamemode creative @s
```

### `/tp <гравець> <координати>`
```
/tp Steve 0 100 0
```

### `/give <гравець> <предмет>`
```
/give Steve diamond 64
```

---

## 🌍 Команди світу

### `/time set <час>`
```
/time set day
/time set night
```

### `/weather <погода>`
```
/weather clear
/weather rain
```

---

## 💾 Команди сервера

### `/save-all` — Зберегти світ
### `/stop` — Зупинити сервер

**Важливо:** Завжди використовуй `/stop` замість закриття вікна!

---

## 🚀 Наступний урок

У **Lesson 6** (фінальному) навчимося **підключати друзів** через Port Forwarding!',
    7,
    5,
    true
  );
  
  RAISE NOTICE 'Lesson 5 створено!';
END $$;

-- ============ LESSON 6 ============
DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 6;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Як підключити друзів до сервера',
    'text',
    '# Підключення Гравців 🌐

Час **запросити друзів**! Розглянемо **4 способи** підключення.

---

## 🏠 Метод 1: Локальна мережа

**Коли працює?**
- ✅ Всі в одній WiFi
- ✅ Не потребує налаштувань
- ✅ Миттєво

**Як?**
1. Дізнайся свій IP: `ipconfig` → IPv4 Address
2. Друзі підключаються: `192.168.1.100:25565`

⚠️ **Обмеження:** Тільки локальна мережа

---

## 🌍 Метод 2: Port Forwarding

**Крок 1:** Статичний IP в налаштуваннях мережі

**Крок 2:** Зайди в роутер (`192.168.1.1`)

**Крок 3:** Налаштуй Port Forwarding:
- External Port: `25565`
- Internal IP: `192.168.1.100`
- Protocol: `TCP + UDP`

**Крок 4:** Дізнайся публічний IP: https://whatismyipaddress.com/

**Крок 5:** Друзі підключаються: `185.123.45.67:25565`

✅ **Тепер доступно з інтернету!**

⚠️ **Безпека:**
- Використовуй `/whitelist on`
- Не давай IP незнайомцям

---

## 🔗 Метод 3: Hamachi (VPN)

**Що це?** Віртуальна локальна мережа через інтернет.

**Як?**
1. Завантаж: https://www.vpn.net/
2. Створи мережу (Network ID + Password)
3. Друзі приєднуються до мережі
4. Підключаються через Hamachi IP: `25.x.x.x`

✅ **Плюси:** Не потрібен Port Forwarding  
❌ **Мінуси:** Макс 5 гравців (безкоштовно)

---

## 🌐 Метод 4: Playit.gg

**Що це?** Публічний домен для локального сервера.

**Як?**
1. Реєстрація: https://playit.gg/
2. Завантаж програму
3. Запусти → отримаєш домен: `myserver.playit.gg`
4. Друзі підключаються через домен

✅ **Плюси:** Безкоштовно, легко  
❌ **Мінуси:** +10-30ms пінг

---

## 📊 Порівняння

| Метод | Складність | Пінг | Безпека |
|-------|------------|------|---------|
| **Локальна мережа** | 🟢 | 1ms | Висока |
| **Port Forwarding** | 🟡 | 5-20ms | Середня |
| **Hamachi** | 🟢 | 20-50ms | Висока |
| **Playit.gg** | 🟢 | 30-80ms | Середня |

---

## 🎉 Курс завершено!

Ти пройшов повний шлях від встановлення Java до підключення друзів!

### Що ти вмієш:
- ✅ Встановлювати Java та Paper
- ✅ Налаштовувати server.properties
- ✅ Використовувати адмін команди
- ✅ Підключати гравців

---

## 🚀 Наступні кроки

**Безкоштовні курси:**
- Курс 2: "Встановлення плагінів"
- Курс 3: "Базова захист сервера"

**Платні курси:**
- Професійна Розробка Плагінів ($5.99)
- Ідеальна Оптимізація ($3.99)
- Network Architecture ($5.99)

**Дякуємо! Бажаємо успіхів з твоїм сервером! 🎮🚀**',
    8,
    6,
    true
  );
  
  RAISE NOTICE 'Lesson 6 створено!';
END $$;

-- ============ ПЕРЕВІРКА ============
SELECT 
  m.title as "Модуль",
  l.title as "Урок",
  l.duration as "Хвилини",
  l.order_index as "Порядок"
FROM course_modules m
JOIN course_lessons l ON l.module_id = m.id::text
WHERE m.course_id = 'free-1'
ORDER BY l.order_index;
