-- =====================================================
-- FREE COURSE 1: Основи створення Minecraft-сервера
-- Lesson 3: Завантаження Paper server.jar
-- =====================================================

DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 3;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Завантаження Paper server.jar',
    'text',
    '# Завантаження Paper Server 📦

Тепер, коли Java встановлена, час завантажити **Paper** — найкращий серверний софт для Minecraft у 2025 році.

---

## 🚀 Чому Paper, а не Vanilla?

| Характеристика | Vanilla | Paper |
|----------------|---------|-------|
| **TPS (20+ гравців)** | 15-17 | 19-20 |
| **RAM (20 гравців)** | 4-6 GB | 2-3 GB |
| **Плагіни** | ❌ Ні | ✅ Так (Spigot/Bukkit) |
| **Оптимізація** | ❌ Базова | ✅ Агресивна |
| **Оновлення** | Рідко | Щотижня |

**Висновок:** Paper швидший, ефективніший, підтримує плагіни.

---

## 📥 Завантаження Paper

### Крок 1: Відкрий офіційний сайт

**Посилання:** https://papermc.io/downloads/paper

---

### Крок 2: Вибери версію Minecraft

**2025 рекомендація:** Обирай **1.21.3** або **останню стабільну** версію.

**Важливо:** Версія сервера має збігатися з версією клієнта гравців!

---

### Крок 3: Завантаж JAR файл

1. Натисни на версію (наприклад, `1.21.3`)
2. Натисни **Download** біля останнього білду (наприклад, `Build #125`)
3. Завантажиться файл: `paper-1.21.3-125.jar` (~30 MB)

---

## 📁 Створення папки сервера

### Windows:

1. Створи нову папку на диску `C:\` або `D:\`:
   ```
   C:\MinecraftServer\
   ```

2. Перемісти завантажений `paper-1.21.3-125.jar` в цю папку

3. **Перейменуй** файл на `server.jar` (для зручності)

**Структура папки:**
```
C:\MinecraftServer\
  └── server.jar
```

---

## ⚙️ Створення start.bat (запуск сервера)

### Крок 1: Створи текстовий файл

У папці `C:\MinecraftServer\` створи новий файл:
- Правий клік → **New** → **Text Document**
- Назва: `start.txt`

---

### Крок 2: Додай код запуску

Відкрий `start.txt` в Notepad і вставь:

```batch
@echo off
title Minecraft Server
java -Xms2G -Xmx4G -XX:+UseG1GC -XX:+ParallelRefProcEnabled -jar server.jar nogui
pause
```

**Пояснення параметрів:**
- `-Xms2G` — **мінімальна RAM** (2 GB)
- `-Xmx4G` — **максимальна RAM** (4 GB)
- `-XX:+UseG1GC` — сучасний збирач сміття (краща продуктивність)
- `nogui` — запуск без графічного інтерфейсу (економія RAM)

**💡 Налаштування RAM:**
| Гравців | Рекомендована RAM | Команда |
|---------|-------------------|---------|
| 2-5 | 2-3 GB | `-Xms1G -Xmx2G` |
| 5-10 | 3-4 GB | `-Xms2G -Xmx4G` |
| 10-20 | 4-6 GB | `-Xms3G -Xmx6G` |
| 20+ | 6-8 GB | `-Xms4G -Xmx8G` |

---

### Крок 3: Збережи як BAT файл

1. **File** → **Save As**
2. **File name:** `start.bat`
3. **Save as type:** `All Files (*.*)`
4. Натисни **Save**

5. **Видали** старий `start.txt`

**Структура папки:**
```
C:\MinecraftServer\
  ├── server.jar
  └── start.bat
```

---

## 🚀 Перший запуск сервера

### Крок 1: Запусти start.bat

Подвійний клік на `start.bat`

**Очікувана поведінка:**
```
[12:34:56 INFO]: Starting minecraft server version 1.21.3
[12:34:56 INFO]: Loading properties
[12:34:56 WARN]: Failed to load eula.txt
[12:34:56 INFO]: You need to agree to the EULA in order to run the server. 
Go to eula.txt for more info.
```

Сервер **зупиниться** — це нормально!

---

### Крок 2: Прийми EULA (ліцензійну угоду)

У папці з''явився файл `eula.txt`. Відкрий його:

```
eula=false
```

**Зміни на:**
```
eula=true
```

**Збережи** файл.

---

### Крок 3: Запусти знову

Подвійний клік на `start.bat`

**Тепер сервер запуститься!** Побачиш:
```
[12:35:10 INFO]: Preparing level "world"
[12:35:15 INFO]: Preparing spawn area: 0%
[12:35:20 INFO]: Preparing spawn area: 10%
...
[12:36:00 INFO]: Done (45.2s)! For help, type "help"
```

✅ **Вітаємо! Сервер працює!** 🎉

---

## 📂 Структура файлів після запуску

```
C:\MinecraftServer\
  ├── server.jar
  ├── start.bat
  ├── eula.txt
  ├── server.properties (налаштування сервера)
  ├── world/ (ігровий світ)
  ├── world_nether/ (Нижній світ)
  ├── world_the_end/ (Край)
  ├── logs/ (логи)
  ├── plugins/ (папка для плагінів)
  └── config/ (конфігурації Paper)
```

---

## 🎮 Як підключитися (локально)?

1. Запусти **Minecraft клієнт** (версія **1.21.3**)
2. **Multiplayer** → **Add Server**
3. **Server Address:** `localhost`
4. **Done** → **Join Server**

✅ Ти маєш зайти на свій сервер!

---

## ⚠️ Частіпроблеми

### Помилка: "Unable to access jarfile server.jar"

**Причина:** Файл `server.jar` не знайдено

**Рішення:**
1. Переконайся що `server.jar` є в папці
2. Перевір що `start.bat` і `server.jar` в **одній папці**

---

### Помилка: "Out of memory"

**Причина:** Недостатньо RAM виділено

**Рішення:** Збільш `-Xmx` в `start.bat`:
```batch
java -Xms2G -Xmx6G ...
```

---

### Сервер зависає на "Preparing spawn area"

**Причина:** Генерація світу займає час (особливо перший раз)

**Рішення:** Дочекайся завершення (2-5 хвилин). При наступних запусках буде швидше.

---

## 📝 Підсумок уроку

Ти навчився:
- ✅ Завантажувати **Paper server.jar** з офіційного сайту
- ✅ Створювати **папку сервера** та структуру файлів
- ✅ Писати **start.bat** з правильними параметрами RAM
- ✅ Приймати **EULA** ліцензію
- ✅ Запускати сервер **вперше**
- ✅ Підключатися локально через `localhost`

---

## 🚀 Наступний урок

У **Lesson 4** ми налаштуємо **server.properties** — основні параметри сервера (порт, режим гри, складність, тощо).

**Готовий налаштовувати сервер? Переходь далі** →',
    6,
    3,
    true
  );
END $$;
