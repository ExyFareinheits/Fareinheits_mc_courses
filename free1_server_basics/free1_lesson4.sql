-- =====================================================
-- FREE COURSE 1: Основи створення Minecraft-сервера
-- Lesson 4: Налаштування server.properties
-- =====================================================

DO $$
DECLARE
  v_module_id TEXT;
  v_lesson_id TEXT;
BEGIN
  SELECT id::text INTO v_module_id 
  FROM course_modules 
  WHERE course_id = 'free-1' AND order_index = 1;

  v_lesson_id := gen_random_uuid()::text;
  
  DELETE FROM course_lessons 
  WHERE module_id = v_module_id AND order_index = 4;
  
  INSERT INTO course_lessons (
    course_id, module_id, lesson_id, title, type, content, duration, order_index, is_free_preview
  ) VALUES (
    'free-1',
    v_module_id,
    v_lesson_id,
    'Налаштування server.properties — основні параметри',
    'text',
    '# Налаштування server.properties ⚙️

Файл `server.properties` — це **головний конфігураційний файл** твого сервера. Тут налаштовуються всі параметри гри.

---

## 📂 Де знайти файл?

```
C:\MinecraftServer\
  └── server.properties
```

Відкрий файл у **Notepad** або **Notepad++**.

---

## 🔧 Основні параметри (ТОП-15)

### 1. `gamemode=survival`

**Режим гри за замовчуванням:**
- `survival` — Виживання (стандарт)
- `creative` — Творчість
- `adventure` — Пригода
- `spectator` — Спостерігач

**Рекомендація:** `survival` для звичайного SMP

---

### 2. `difficulty=easy`

**Складність гри:**
- `peaceful` — Мирна (без мобів)
- `easy` — Легка
- `normal` — Нормальна
- `hard` — Важка

**Рекомендація:** `normal` для балансу

---

### 3. `pvp=true`

**Дозволити PvP (гравець проти гравця)?**
- `true` — Гравці можуть атакувати один одного
- `false` — Гравці НЕ можуть завдати шкоди іншим

**Рекомендація:** `false` для дружнього SMP

---

### 4. `max-players=20`

**Максимальна кількість гравців онлайн.**

**Рекомендація:**
| RAM | max-players |
|-----|-------------|
| 2 GB | 5-10 |
| 4 GB | 10-20 |
| 6 GB | 20-30 |
| 8 GB | 30-50 |

---

### 5. `server-port=25565`

**Порт сервера.**

- Стандартний порт: `25565`
- Гравці підключаються: `IP:25565`

**Рекомендація:** Не змінюй, якщо не знаєш навіщо

---

### 6. `online-mode=true`

**Перевірка автентифікації Mojang:**
- `true` — Тільки ліцензовані акаунти (рекомендовано)
- `false` — Будь-які акаунти (небезпечно!)

**Рекомендація:** `true` (захист від чітерів)

---

### 7. `white-list=false`

**Білий список (дозволені гравці):**
- `true` — Тільки додані гравці можуть зайти
- `false` — Будь-хто може зайти

**Рекомендація:** `true` для приватного сервера

---

### 8. `motd=A Minecraft Server`

**Message of the Day** — повідомлення в списку серверів.

**Приклад з кольорами:**
```
motd=§a§l🌿 Ukrainian SMP §r§7| §eВиживання §71.21.3
```

**Коди кольорів:**
- `§0` — Чорний
- `§1` — Темно-синій
- `§2` — Темно-зелений
- `§3` — Темно-бірюзовий
- `§4` — Темно-червоний
- `§5` — Фіолетовий
- `§6` — Золотий
- `§7` — Сірий
- `§8` — Темно-сірий
- `§9` — Синій
- `§a` — Зелений
- `§b` — Бірюзовий
- `§c` — Червоний
- `§d` — Рожевий
- `§e` — Жовтий
- `§f` — Білий

**Стилі:**
- `§l` — Жирний
- `§o` — Курсив
- `§n` — Підкреслений
- `§r` — Скинути стиль

---

### 9. `view-distance=10`

**Дальність видимості чанків (1 чанк = 16 блоків).**

| view-distance | Дальність | RAM | TPS |
|---------------|-----------|-----|-----|
| 6 | 96 блоків | Низьке | Високий |
| 8 | 128 блоків | Середнє | Стабільний |
| 10 | 160 блоків | Високе | Середній |
| 12+ | 192+ блоків | Дуже високе | Низький |

**Рекомендація:** `8` для балансу продуктивності

---

### 10. `spawn-protection=16`

**Радіус захисту спавну (в блоках).**

- Тільки ОП можуть будувати в цій зоні
- Рекомендація: `0` (вимкнути) або `16`

---

### 11. `allow-nether=true`

**Дозволити Nether (Нижній світ)?**

**Рекомендація:** `true`

---

### 12. `enable-command-block=false`

**Дозволити Command Blocks?**

**Рекомендація:** `false` (захист від експлойтів)

---

### 13. `resource-pack=`

**Посилання на текстур-пак.**

**Приклад:**
```
resource-pack=https://example.com/resourcepack.zip
```

**Рекомендація:** Залиш порожнім

---

### 14. `level-name=world`

**Назва папки світу.**

**Рекомендація:** `world` (стандарт)

---

### 15. `level-seed=`

**Seed світу (для генерації).**

**Приклад:**
```
level-seed=1234567890
```

**Рекомендація:** Залиш порожнім (випадковий seed)

---

## 📝 Приклад оптимізованого server.properties

```properties
gamemode=survival
difficulty=normal
pvp=false
max-players=20
server-port=25565
online-mode=true
white-list=true
motd=§a§l🌿 Ukrainian SMP §r§7| §eВиживання
view-distance=8
spawn-protection=0
allow-nether=true
allow-flight=false
enable-command-block=false
level-name=world
```

---

## 🎮 Реальний кейс: "Lag Fix"

**Ситуація:** Сервер лагає при 15+ гравцях (TPS падає до 15)

**Налаштування до:**
```
view-distance=16
max-players=30
```

**Зміни:**
```
view-distance=8
max-players=20
```

**Результат:**
- TPS: 15.3 → 19.8 ✅
- RAM: 4.5GB → 3.2GB ✅
- Пінг гравців: -15ms ✅

**Висновок:** `view-distance=8` — золота середина!

---

## 📝 Підсумок уроку

Ти навчився:
- ✅ Редагувати **server.properties**
- ✅ Налаштовувати **15 основних параметрів**
- ✅ Використовувати **кольори в MOTD**
- ✅ Оптимізувати **view-distance** для кращого TPS
- ✅ Застосовувати **білий список** для безпеки

---

## 🚀 Наступний урок

У **Lesson 5** ми вивчимо **базові адмін команди** (/op, /kick, /ban, /whitelist, тощо).

**Готовий стати адміністратором? Переходь далі** →',
    8,
    4,
    true
  );
END $$;
