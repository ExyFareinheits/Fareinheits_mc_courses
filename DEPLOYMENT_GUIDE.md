# 🚀 Гайд по деплою на Vercel

## 📋 Передумови

- [x] Всі безкоштовні курси заповнені контентом (free-1 до free-6)
- [x] Головна сторінка оновлена з фільтрами та красивим дизайном
- [x] Сайдбар уроків оптимізований з центральним меню
- [x] vercel.json налаштований для SPA routing
- [x] TypeScript помилки виправлені
- [x] .env.example створений

## 🔧 Крок 1: Підготовка проекту

### 1.1 Перевірка білду

```bash
npm run build
```

Переконайтеся, що немає помилок і папка `dist` створена.

### 1.2 Перевірка preview

```bash
npm run preview
```

Відкрийте http://localhost:4173 і перевірте:
- ✅ Головна сторінка завантажується
- ✅ Курси відображаються з фільтрами (Всі/Безкоштовні/Платні)
- ✅ Сторінка курсу відкривається
- ✅ Сторінка уроків працює з сайдбаром

## 📦 Крок 2: Git та GitHub

### 2.1 Ініціалізація Git (якщо ще не зроблено)

```bash
git init
git add .
git commit -m "Initial commit: Ready for Vercel deployment"
```

### 2.2 Створення GitHub репозиторію

1. Відкрийте https://github.com/new
2. Назва: `Fareinheits_mc_courses`
3. Опис: `Minecraft Server Administration Courses Platform`
4. Visibility: Public або Private
5. **НЕ** додавайте README, .gitignore, license

### 2.3 Підключення до GitHub

```bash
git remote add origin https://github.com/YOUR_USERNAME/Fareinheits_mc_courses.git
git branch -M main
git push -u origin main
```

## 🌐 Крок 3: Деплой на Vercel

### 3.1 Створення Vercel акаунту

1. Відкрийте https://vercel.com/signup
2. Увійдіть через GitHub
3. Авторизуйте Vercel для доступу до репозиторіїв

### 3.2 Імпорт проекту

1. Натисніть **"Add New Project"**
2. Виберіть `Fareinheits_mc_courses` репозиторій
3. Налаштування:
   - **Framework Preset**: Vite
   - **Root Directory**: `./`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### 3.3 Додавання змінних середовища

В **Environment Variables** додайте:

```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

**Де взяти ключі?**
1. Відкрийте https://supabase.com/dashboard
2. Виберіть свій проект
3. Settings → API
4. Скопіюйте:
   - Project URL → `VITE_SUPABASE_URL`
   - anon/public key → `VITE_SUPABASE_ANON_KEY`

### 3.4 Деплой

Натисніть **"Deploy"** і чекайте 2-3 хвилини.

## ✅ Крок 4: Перевірка після деплою

### 4.1 Базова функціональність

Відкрийте ваш Vercel URL (наприклад, `fareinheits-mc-courses.vercel.app`):

- [ ] Головна сторінка завантажується
- [ ] Фільтри курсів працюють (Всі/Безкоштовні/Платні)
- [ ] Статистика відображається (10+ курсів, 6 безкоштовних)
- [ ] Кнопки "Переглянути курси" працюють
- [ ] Адаптивність на мобільних

### 4.2 Навігація

- [ ] Меню навігації працює
- [ ] Маршрути `/courses`, `/about`, `/login` працюють
- [ ] Назад/вперед браузера працюють

### 4.3 Курси

- [ ] Список курсів завантажується
- [ ] Картки курсів відображаються правильно
- [ ] Перехід на сторінку курсу працює
- [ ] Опис курсу, теми, ціна відображаються

### 4.4 Уроки (якщо є доступ)

- [ ] Сайдбар відкривається/закривається кнопкою
- [ ] Список уроків відображається по центру
- [ ] Прогрес уроків зберігається
- [ ] Перехід між уроками працює

## 🗄️ Крок 5: Supabase налаштування

### 5.1 Виконання SQL міграцій

1. Відкрийте Supabase Dashboard
2. SQL Editor
3. Виконайте по черзі:

#### rls_course_tables.sql

```sql
-- Дозволити публічний SELECT для курсів, модулів, уроків
ALTER TABLE courses ENABLE ROW LEVEL SECURITY;
ALTER TABLE modules ENABLE ROW LEVEL SECURITY;
ALTER TABLE lessons ENABLE ROW LEVEL SECURITY;
ALTER TABLE quizzes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow public to read courses" ON courses FOR SELECT USING (true);
CREATE POLICY "Allow public to read modules" ON modules FOR SELECT USING (true);
CREATE POLICY "Allow public to read lessons" ON lessons FOR SELECT USING (true);
CREATE POLICY "Allow public to read quizzes" ON quizzes FOR SELECT USING (true);
```

#### free1_*.sql (якщо ще не виконано)

Виконайте всі SQL файли з папки курсів для заповнення бази даних контентом.

### 5.2 Перевірка підключення

```bash
# В консолі браузера на вашому сайті
localStorage.getItem('supabase.auth.token')
```

Якщо `null` — все ок для публічного доступу.

## 🔄 Крок 6: Автоматичні оновлення

### 6.1 Git workflow

Тепер кожен push на GitHub автоматично деплоїть на Vercel:

```bash
git add .
git commit -m "Update: Added new course content"
git push
```

Vercel автоматично:
1. Зібере проект
2. Запустить білд
3. Задеплоїть нову версію
4. Надасть preview URL

### 6.2 Preview deployments

Кожна гілка отримує окремий preview URL:

```bash
git checkout -b feature/new-course
git add .
git commit -m "Add new course"
git push -u origin feature/new-course
```

Vercel створить preview URL типу `fareinheits-mc-courses-git-feature-new-course.vercel.app`

## 🎯 Крок 7: Налаштування домену (опціонально)

### 7.1 Купівля домену

Рекомендовані реєстратори:
- Namecheap (від $8.88/рік)
- Google Domains
- Cloudflare Registrar

### 7.2 Підключення до Vercel

1. Vercel Dashboard → Settings → Domains
2. Додайте ваш домен: `mcserverlessons.com`
3. Налаштуйте DNS записи:

```
Type: A
Name: @
Value: 76.76.21.21

Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

4. Чекайте 24-48 годин на DNS propagation

## 🐛 Troubleshooting

### Помилка: "404 Page Not Found" на маршрутах

**Рішення**: Перевірте `vercel.json`:

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

### Помилка: "Failed to load module"

**Рішення**: Очистіть cache і пересоберіть:

```bash
rm -rf dist node_modules
npm install
npm run build
```

### Помилка: Supabase не підключається

**Рішення**: 
1. Перевірте Environment Variables у Vercel
2. Переконайтеся, що ключі починаються з `VITE_`
3. Redeploy після додавання змінних

### Помилка: TypeScript помилки при білді

**Рішення**:

```bash
npm run build 2>&1 | grep error
```

Виправте помилки та закомітьте.

## 📊 Моніторинг

### Vercel Analytics

1. Vercel Dashboard → Analytics
2. Перегляньте:
   - Відвідування
   - Performance (Web Vitals)
   - Помилки 404

### Supabase Analytics

1. Supabase Dashboard → Statistics
2. Перегляньте:
   - API requests
   - Database size
   - Active connections

## 🎉 Готово!

Ваш проект задеплоєно! Тепер доступний за адресою:
- Production: `https://fareinheits-mc-courses.vercel.app`
- Custom domain: `https://your-domain.com` (якщо налаштували)

### Наступні кроки:

1. Додайте посилання на сайт в соцмережах
2. Створіть Discord сервер для спільноти
3. Додайте Google Analytics
4. Створіть перші уроки з відео контентом
5. Налаштуйте монетизацію (Gumroad, LiqPay)

---

**Потрібна допомога?** Створіть issue на GitHub або напишіть у Discord.
